# 📋 За Димитър (2026-06-12)

> Две секции: какво е **тествано** и какво **следва за тестване**.
> „Следва за тестване" е разделено на: **А)** неизпробвана функционалност
> (ръчно/на реално устройство) и **Б)** какво остава **ти** да направиш.
>
> Бележка за бранда: по твое решение Intima вече **не е** „privacy" app.
> Партньорският чат, снимките и видеата се пазят на нашия сървър в явен
> вид и това е **оповестено** в Privacy Policy и Play Data Safety.
> Дневникът и календарът остават криптирани само на устройството.

---

## ✅ ТЕСТВАНО

> „Тествано" = покрито от автоматични тестове и/или проверено на
> Pixel 7 емулатора (стартиране без крашове). Реално устройство — виж А).

- **74 host теста зелени** + `flutter analyze` без проблеми.
- **Фази 0–4** (данни, сигурност, календар, дневник) — логиката е
  тествана и беше потвърдена от теб по-рано на устройство.
- **Premium логика**: Premium флаг, PDF генерация (кирилица), палитри
  (Intima + Роуз голд безплатни; Среднощно синьо + Океан зад paywall),
  инсайти (реален цикъл, фази, тренд, рекап), stealth предпазители.
- **Шанс за забременяване**: изчислението по дни от овулацията +
  `daysFromOvulation` — покрито с тестове.
- **Партньорска логика** (in-memory): сдвояване, чат текст/снимка/видео,
  няколко партньора, „мое/чуждо", прекъсване, псевдоним — 10 теста.
- **Език, тема, профил** контролери: персистентност — тествани.
- **Фаза 8 — пози**: каталог (филтри), статус/оценка/бележка/„пробвано на",
  безплатен старт пакет + заключени Premium колекции — логиката е тествана.
  **21 пози** с **професионално изглеждащи генерирани векторни илюстрации**
  (силует на двойка в прегръдка, градиент, дълбочина — различни за всяка;
  визуално прегледани чрез рендиране в PNG).
- **Фаза 9 — локални корелации** (настроение ↔ фертилни дни, дял интимност,
  либидо ↔ енергия) + **персонализирани журнал промптове** по фаза — тествани.
- **Couple Match** (Фаза 8): взаимен интерес към поза → 💘; разкриват се
  само взаимните пози — 12 партньорски теста (вкл. „само взаимни" и
  „махане на интерес").
- Приложението **стартира на емулатора** с новия 5-табов навигатор
  (Календар · Дневник · Инсайти · Партньор · Профил) и инсталираният билд
  включва всичко по-горе (82 host теста зелени).

---

## 🔜 СЛЕДВА ЗА ТЕСТВАНЕ

### А) Неизпробвана функционалност (ръчно / реално устройство)

> Подробните стъпки за част от тях са в `RACHNI_TESTOVE.md`.

1. **Партньорски чат на живо** — работи чак след като пуснеш новата
   `supabase/schema.sql` (виж Б1). После: Профил→Партньор → „Покани" на
   едното устройство, „Имам код" на другото → чат с текст/снимки/видео.
2. **Качване на снимки/видео в чата** — реална галерия, по-големи файлове.
3. **Подробен изглед на ден** — тапни ден в календара: фаза, записано,
   шанс за забременяване + дисклеймър; бутон „Запиши/редактирай".
4. **Овулация** — легендата казва „Овулация" с ℹ️ обяснение.
5. **Профил** — аватар от реална галерия, смяна на име; настройките са
   вътре в Профила.
6. **Вход с Facebook/Google** — само след Б2 (untested дотогава).
7. **Аудио бележки, видео в дневника, палитри, инсайти** — ръчно по
   чеклиста (логиката е тествана, UI потокът — не на устройство).
8. **Биометрия, реална доставка на нотификации, release билд** —
   изискват реално устройство (емулаторът не ги покрива).
9. **Библиотека с пози (Фаза 8)** — Профил → „Библиотека с пози":
   филтри, отбелязване (искам/пробвано/любимо), оценка, бележка,
   „пробвано на" дата; заключените колекции водят към paywall.
   **Couple Match на живо**: на 2 устройства, свързани като двойка,
   маркирайте една и съща поза „искам да пробвам" → 💘 (след Б1).
10. **Инсайти „Какво забелязваме" (Фаза 9)** — картата с корелации
    се появява при достатъчно данни; персонализираната подсказка в
    редактора на нов запис („Подсказка за днес").

### Б) Какво остава ти да направиш

**Б1. Supabase — пусни обновената схема (1 мин)** ⚠️ задължително за чата
И ЗА COUPLE MATCH
- Dashboard → SQL Editor → New query → копирай ЦЕЛИЯ `supabase/schema.sql`
  → Run. (Дропва старите таблици и слага: `messages`, storage bucket
  `partner-media`, **`pose_interests` + функция `pose_matches`** за Couple
  Match, RLS, функции.) Файлът е re-runnable — пусни го пак сега.
- Анонимният вход вече е включен ✅.

**Б2. (По желание) Вход с Facebook/Google**
- Facebook: създай Facebook app → в Supabase → Authentication → Providers
  → Facebook → сложи App ID/Secret + redirect URL. Тогава бутонът работи.
- Google: подобно (Google Cloud OAuth client).
- ⚠️ **Instagram и TikTok не се поддържат** от Supabase auth — не мога да
  ги вържа без отделна, тежка custom интеграция и одобрение от платформите.
  Препоръчвам да караме с Facebook/Google (+ Apple за iOS).

**Б2½. Фаза 8 — пози: реален CC0 арт (остава 1 стъпка за теб)**
- Готов е **drop-in pipeline за CC0 SVG**: сложиш ли SVG файл, кръстен с
  id-то на позата, в `app/assets/poses/` → приложението го показва
  автоматично (бяло, върху брандирания градиент). Липсва ли — ползва се
  генерираният силует (fallback). Нищо не гърми.
- ⚠️ **Твоята стъпка (2 мин в браузър):** ботът ми е блокиран от
  Cloudflare на SVG Repo, но в браузър работи. Влез в
  https://www.svgrepo.com/ , филтрирай по лиценз **CC0**, изтегли иконите,
  които харесаш, преименувай ги на `<pose-id>.svg` и ги пусни в
  `app/assets/poses/`. Списъкът с id-та е в `app/assets/poses/README.md`.
  После казваш „готово" и аз пребилдвам — появяват се.
- ⚠️ **Google Play:** явни изображения на сексуални пози (дори икони) може
  да нарушат политиката за сексуално съдържание. По-безопасно е да избереш
  дискретни/неексплицитни силуети. Изборът на съдържание е твой.
- Защо не ги свалих сам: SVG Repo (единственият добър CC0 източник за това)
  блокира автоматизирани заявки; Openclipart API е мъртво; GitHub набор
  няма; а чуждите/explicit изображения = бан в Play. Платен/поръчков сет
  остава опция (тогава само ми дай файловете).
- 💳 **Покупка на тематични пакети (IAP)** — изисква Play billing акаунт
  (както Premium-а). Сега заключените колекции отварят paywall-а; реалната
  еднократна покупка чака billing.
- ✅ **Couple Match — готово** (2026-06-13): при взаимно „искам да пробвам"
  от двамата партньори се показва дискретно 💘 (само взаимните пози; твоите
  несподелени желания не се виждат). Чака само пускането на новата схема
  (Б1). Реално push известие, докато партньорът маркира, а ти не си в
  приложението, изисква FCM (не е правено) — засега съвпадението изскача
  при следващо отваряне/маркиране.

**Б2¾. Фаза 9 — AI: какво зависи от теб**
- Сега инсайтите/корелациите/промптовете са **100% на устройството** (без
  AI, нищо не напуска телефона) — затова не чакат нищо от теб.
- Ако искаш **истински AI** (LLM обобщения, „Wrapped" текстове): трябва
  външна услуга (Claude API ключ) + изричен opt-in екран за съгласие +
  бюджет за разходи. Това е продуктово/правно решение — кажи и го изграждам
  с ясно съгласие какво се изпраща.

**Б3. Google Play Console** (след верификацията на самоличността)
- Create app → заглавие (виж Б6), език по подразбиране: български.
- Качи bundle: `app\build\app\outputs\bundle\release\app-release.aab`
  (Internal testing първо).
- Store listing: текстове от `docs/store/PLAY_STORE_LISTING.md`.
- Скрийншоти от `docs/store/screenshots/`; feature graphic в `app/assets/store/`.
- **Data Safety**: отговорите са обновени в листинг файла — вече
  декларираме, че събираме чат/медия (само за функция „Партньор").
- IARC въпросник: интимна тематика, без графично съдържание в приложението.

**Б4. Физически неща**
- ⚠️ **БЕКЪП НА KEYSTORE** — папка `C:\Users\dimitar.k.boychev\intima-release-keys\`
  + `app\android\key.properties` → в парола мениджър/облак. Загубен = край на ъпдейтите.
- **Реално Android устройство** за финалния тест (А8).

**Б5. Privacy policy хостинг**
- Текстът е обновен (`docs/store/PRIVACY_POLICY.md`) — попълни
  `[ИМЕ/ФИРМА]` и `[КОНТАКТЕН ИМЕЙЛ]`. Кажи „качи я" и я качвам на GitHub
  Pages; после слагаш URL-а в Console.

**Б6. Решения, които чакат теб**
- [ ] **Заглавие в Play**: „Intima — интимен календар" / „Intima — Cycle & Diary"
- [ ] **Контактен имейл** (нов отделен Gmail, напр. intima.app.bg@gmail.com)
- [ ] **Име на разработчика** (лично/фирма) за privacy policy
- [ ] **Кои още Premium функции** влизат — пълен списък в `IDEI_FUNKTSII.md`
      (20 Premium + 10 free). Моят топ: дискретни икони, widget „луна",
      капсула на времето.

---

## 🗓️ Хронология (свършено)

- **2026-06-12**: репозициониране като обикновено приложение; партньорски
  чат със снимки/видео + няколко партньора (Supabase, без E2E, оповестено);
  Партньор и Профил табове; подробен изглед на ден + шанс за забременяване;
  аватар/профил; вход скеле (FB/Google); аудио бележки; палитри; инсайти;
  видео в дневника; „Овулация"; избор на език.
- **2026-06-11**: stealth с фалшив PIN; светла тема; Play скрийншоти (BG+EN).
